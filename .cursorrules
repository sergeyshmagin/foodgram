# 🏆 Cursor AI Rules для проекта Foodgram

## 📋 Основные принципы
- **Язык**: Всегда отвечаю на русском языке
- **Стиль кода**: Строго следую PEP8
- **Размер файлов**: Один файл ≤ 500 строк. При превышении — рефакторинг по модулям
- **Тесты**: Покрытие тестами, комментарии и документация — сразу, не откладывать
- **Секреты**: API-ключи только через переменные окружения, никогда в коде

## 🛠 Технологический стек
**Основные фреймворки:**
- Django 3.2.16 + Django REST Framework 3.12.4
- FastAPI (при необходимости)
- PostgreSQL

## 🔧 Правила разработки

### Терминал и окружение
- Перед выполнением команд всегда проверяй активность виртуального окружения
- Используй `edit_file` для исправления файлов

- Модели всегда должны соответствовать структуре БД

### Импорты (PEP8)
```python
# Строго в таком порядке:
# 1. Стандартные библиотеки
import os
from datetime import datetime

# 2. Сторонние библиотеки
from django.db import models
from rest_framework import serializers

# 3. Модули проекта
from apps.users.models import User
from .constants import MAX_LENGTH
```

### Константы и магические числа
```python
# ✅ Все константы в constants.py
MAX_NAME_LENGTH = 200
MIN_COOKING_TIME = 1
COMMENTS_LIMIT = 10

# ❌ Не используй магические числа в коде
```

### Модели Django
```python
# ✅ Правильная структура модели
class Recipe(models.Model):
    name = models.CharField(max_length=MAX_NAME_LENGTH, verbose_name='Название')
    cooking_time = models.PositiveIntegerField(
        verbose_name='Время приготовления',
        validators=[MinValueValidator(MIN_COOKING_TIME)]
    )
    
    class Meta:
        verbose_name = 'Рецепт'
        verbose_name_plural = 'Рецепты'
        ordering = ['-pub_date']
    
    def __str__(self):
        return self.name
```

### Абстрактные модели
```python
# ✅ Выноси повторяющиеся поля в абстрактную модель
class TimeStampedPublishedModel(models.Model):
    created_at = models.DateTimeField(auto_now_add=True)
    is_published = models.BooleanField(default=False)
    
    class Meta:
        abstract = True
```

### QuerySet и Manager
```python
# ✅ Используй кастомные QuerySet
class RecipeQuerySet(models.QuerySet):
    def published(self):
        return self.filter(is_published=True)
    
    def by_author(self, author):
        return self.filter(author=author)

class Recipe(models.Model):
    objects = RecipeQuerySet.as_manager()
```

### Админка Django
```python
# ✅ Правильная регистрация с @admin.register
@admin.register(Recipe)
class RecipeAdmin(admin.ModelAdmin):
    list_display = ['name', 'author', 'cooking_time', 'favorites_count']
    list_filter = ['tags', 'pub_date']
    search_fields = ['name', 'text']
    
    @admin.display(description='Количество в избранном')
    def favorites_count(self, obj):
        return obj.favorited_by.count()

# Для кастомной модели User
@admin.register(User)
class CustomUserAdmin(UserAdmin):
    list_display = ['email', 'username', 'first_name', 'last_name', 'is_staff']
    search_fields = ['email', 'username']
    ordering = ['email']

# Удаляй ненужные модели
from django.contrib.auth.models import Group
admin.site.unregister(Group)
```

### Сериализаторы
```python
# ✅ Не переопределяй create() без необходимости
class RecipeSerializer(serializers.ModelSerializer):
    tags = serializers.PrimaryKeyRelatedField(
        queryset=Tag.objects.all(),
        many=True
    )
    author = serializers.PrimaryKeyRelatedField(read_only=True)
    
    class Meta:
        model = Recipe
        fields = '__all__'
        validators = [
            UniqueTogetherValidator(
                queryset=Recipe.objects.all(),
                fields=['author', 'name']
            )
        ]
    
    def validate_cooking_time(self, value):
        if value < 1:
            raise serializers.ValidationError(
                'Время приготовления должно быть больше 0'
            )
        return value
    
    # ✅ Используй to_representation() для изменения формата ответа
    def to_representation(self, instance):
        representation = super().to_representation(instance)
        representation['favorites_count'] = instance.favorited_by.count()
        return representation

# ❌ Пустые сериализаторы .save() без логики — бессмысленны
```

### ViewSets и оптимизация
```python
# ✅ Правильная структура ViewSet
class RecipeViewSet(viewsets.ModelViewSet):
    serializer_class = RecipeSerializer
    permission_classes = [IsAuthenticated, IsOwnerOrReadOnly]
    filter_backends = [DjangoFilterBackend]
    filterset_class = RecipeFilter
    
    def get_queryset(self):
        # Оптимизируй запросы с select_related и prefetch_related
        return Recipe.objects.select_related('author').prefetch_related(
            'ingredients', 'tags'
        ).annotate(
            favorites_count=Count('favorited_by'),
            is_favorited=Exists(
                Favorite.objects.filter(
                    user=self.request.user,
                    recipe=OuterRef('pk')
                )
            )
        )

# ❌ Используй annotate() вместо SerializerMethodField для избежания N+1 queries
```

### Permissions
```python
# ✅ Все permissions в permissions.py
class IsOwnerOrReadOnly(permissions.BasePermission):
    """НЕ включает is_authenticated - используется отдельно."""
    def has_object_permission(self, request, view, obj):
        return (
            request.method in permissions.SAFE_METHODS
            or obj.author == request.user
        )

# ✅ Используй permissions отдельно
class RecipeViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated, IsOwnerOrReadOnly]
```

### Фильтры
```python
# ✅ Все фильтры в filters.py
class RecipeFilter(django_filters.FilterSet):
    is_favorited = django_filters.BooleanFilter(
        method='filter_is_favorited'
    )
    tags = django_filters.ModelMultipleChoiceFilter(
        field_name='tags__slug',
        to_field_name='slug',
        queryset=Tag.objects.all()
    )
    
    def filter_is_favorited(self, queryset, name, value):
        if self.request.user.is_authenticated and value:
            return queryset.filter(favorited_by=self.request.user)
        return queryset
    
    class Meta:
        model = Recipe
        fields = ['author', 'tags']
```

### Тестирование (pytest)
```python
# ✅ Фикстуры в conftest.py
@pytest.fixture(autouse=True)
def enable_db_access(db):
    """Автоматический доступ к БД во всех тестах."""
    pass

@pytest.fixture
def api_client():
    return APIClient()

# ✅ Все reverse() как фикстуры
@pytest.fixture  
def recipe_url():
    return reverse('recipe-list')

# ✅ Используй @pytest.mark.parametrize везде с повторяющимися данными
@pytest.mark.parametrize(
    'cooking_time,expected_status',
    [
        (0, 400),      # невалидное значение
        (1, 201),      # минимальное валидное
        (60, 201),     # нормальное значение
        (-5, 400),     # отрицательное
    ]
)
def test_recipe_cooking_time_validation(api_client, user, cooking_time, expected_status):
    # тест логики

# ✅ Bulk создание данных
@pytest.fixture
def many_recipes():
    recipes = []
    for i in range(RECIPES_LIMIT):
        recipes.append(Recipe(
            name=f'Recipe {i}',
            cooking_time=30,
            pub_date=timezone.now() - timedelta(days=i)
        ))
    return Recipe.objects.bulk_create(recipes)

# ✅ Используй constants.py даже в тестах
```

### Базовый класс для UnitTest
```python
# ✅ BaseTestCase в common.py
class BaseTestCase(TestCase):
    @classmethod
    def setUpTestData(cls):
        cls.user = User.objects.create_user(
            email='test@example.com',
            password='testpass123'
        )
        cls.recipe_url = reverse('recipe-list')
    
    def authenticate_user(self):
        self.client.force_authenticate(user=self.user)
```

### API структура
```python
# ✅ Версионирование API
urlpatterns = [
    path('api/v1/', include('api.v1.urls')),
]

# ✅ ViewSet с базовым классом для категорий и жанров
class BaseListViewSet(
    mixins.ListModelMixin,
    mixins.CreateModelMixin,
    mixins.DestroyModelMixin,
    viewsets.GenericViewSet
):
    """Базовый класс для категорий и жанров."""
    pass
```

### Безопасность
```python
# ✅ Переменные окружения
SECRET_KEY = os.environ.get('SECRET_KEY')
DEBUG = os.environ.get('DEBUG', 'False') == 'True'

# ❌ Никогда не хардкодь секреты
```

### Логирование
```python
# ✅ Не логируй ошибки в нижнем уровне — прокидывай наверх
def low_level_function():
    try:
        # логика
        pass
    except DatabaseError:
        # НЕ логируем здесь, прокидываем выше
        raise

def high_level_function():
    try:
        low_level_function()
    except DatabaseError as e:
        logger.error(f"Database error: {e}")
        # обрабатываем
```

### Качество кода
```python
# ✅ Используй кавычки одного типа во всём проекте
GOOD_STRING = 'Используйте одинарные кавычки везде'

# ✅ Удаляй пустые классы/методы или добавляй docstring
class EmptyView:
    """Пустой view для будущей реализации."""
    pass

# ✅ Все ошибки — через конкретные исключения
def validate_data(data):
    if not isinstance(data, dict):
        raise TypeError("Data must be a dictionary")
    if 'name' not in data:
        raise ValidationError("Name is required")
```

### Requirements структура
```
requirements/
├── base.txt          # Основные зависимости
├── development.txt   # Для разработки
└── production.txt    # Для продакшена
```

### Settings структура
```
settings/
├── __init__.py
├── base.py           # Общие настройки
├── development.py    # Для разработки
├── production.py     # Для продакшена
└── testing.py        # Для тестов
```

### Docker
```dockerfile
# ✅ Не используй root пользователя
FROM python:3.8-slim

RUN useradd --create-home --shell /bin/bash app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app
WORKDIR /app
USER app
```

### README.md обязательные разделы
```markdown
# Проект Foodgram

## 📖 Описание проекта
## 🚀 Установка и запуск
## 📡 Примеры API-запросов
## 🔗 Полезные ссылки
## 📄 Лицензия
```

## 🚫 Что НЕ делать
- НЕ править миграции вручную
- НЕ использовать магические числа
- НЕ хардкодить секреты
- НЕ создавать N+1 queries
- НЕ логировать в нижнем уровне
- НЕ создавать пустые методы без логики
- НЕ запускать Docker от root
- НЕ включать is_authenticated в IsAdminOrReadOnly/IsOwnerOrReadOnly

## 🎯 Цель
Создать качественный, безопасный и масштабируемый проект Foodgram с соблюдением всех лучших практик Django и Python разработки. 